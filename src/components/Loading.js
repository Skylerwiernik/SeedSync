const Loading = () => {
    return (
        <div className="flex justify-center items-center h-screen bg-red-50">
            <div className="animate-spin rounded-full h-16 w-16 border-t-4 border-red-900"></div>
        </div>
    );
};

export default Loading;